#include "Spring.hpp"

Spring::Spring(Shader* shader = NULL) : Object(shader) {

}

Spring::Spring(FLOAT l, FLOAT stiff, Particule *part1, Particule *part2, Shader* shader = NULL) :
  Object(shader), l0(l), k(stiff), p1(part1), p2(part2) {

}

  void Spring::animate();
  void Spring::draw(glm::mat4 m = glm::mat4(1.0f), Shader *s = NULL);
  
  FLOAT Spring::getLength() const;
  void Spring::updateForces();
